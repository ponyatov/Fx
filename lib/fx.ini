# line comment

640 `W = 480 `H = gui ;
sound                 ;

audio.in.0 open
? repl

audio.in.0 dup open ? record
1000 delay
audio.out.0 dup open play
? repl ;

/out halt ? repl halt gui

34 ?

# repl

nop halt
